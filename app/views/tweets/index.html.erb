<div class="contents row">
  <% @tweets.each do |tweet| %>
    <div class="content_post" style="background-image: url(<%= tweet.image %>);">
      <%= simple_foemat(tweet.text)Papp %>
      <span class="name">
        <%= tweet.name %>  
      </span>
    </div>
  <%= end %>
</div>